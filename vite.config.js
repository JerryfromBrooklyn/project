export default {
  server: {
    port: 9000,
    strictPort: false, // Allow trying other ports if 9000 is in use
    host: true, // Expose to all networks
    open: true // Open browser automatically
  }
} 